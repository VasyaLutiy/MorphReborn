// This file lives under node_modules and MUST be excluded by the
// whole-project source filter. If it ever appears in compiled context,
// the filter regressed.
module.exports = function junk() {
  return "should never be in context";
};
