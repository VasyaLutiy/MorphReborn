# Предрегистрация: ночная колода (эпик #1)

> Записано **до** сборки колоды, автором спеки
> [`TASK_nightly_deck.md`](./TASK_nightly_deck.md), и намеренно вынесено из неё:
> число карт и поколений в спеке заякорило бы оркестратора, который спеку
> читает. Оркестратор этот файл не читает — ему выдаются ровно два документа,
> `Morph_SKILL.md` и спека.
>
> Дерево: `main` = `2a6618e`, чистое, 603 теста. Исполнитель `@glm`.

## Прогноз автора спеки

| величина | прогноз |
|---|---|
| карт в колоде | 8 |
| поколений | 2 |
| карт с перегенерацией | 2 |
| карт, провалившихся насовсем | 0 |
| конфликтов `write-write` на префлайте | 0 |
| карт с исходом `stale-context` | 0 |
| тестов после прогона | > 603 |
| счёт исполнителя | < $0.20 |

## Форма, которую я ожидаю увидеть

Скетч автора спеки, сделанный до передачи оркестратору. Записан **для сравнения
с фактом**, не как указание: совпадение или расхождение с тем, что соберёт
оркестратор, и есть замер стоимости разбивки.

- **Поколение 1, широкое, карты на разных файлах:** обрезка диагноза
  (`cards/acceptance.py`); новый модуль бюджета; новый модуль нотификатора;
  заказ в git (владелец `cards/store.py`); устойчивость REPL — либо сразу
  проводкой, либо новым модулем.
- **Поколение 2, только проводка, только через `depends_on`:** бюджет в
  безголовый путь; нотификатор в конец прогона; ожидание в `flows/morph.py`.

## Проверяемые утверждения

1. Ни одна карта не провалится по `stale-context`.
2. Оркестратор не поставит обрезку диагноза отдельным первым поколением
   (ограничение 8 спеки: починка не действует на текущий прогон).
3. Ни одна карта не будет править `flows/morph.py` шире, чем на 60 строк.

## Факт

| величина | прогноз | факт |
|---|---|---|
| карт в колоде | 8 | **8** ✓ |
| поколений | 2 | 3 ✗ |
| карт с перегенерацией | 2 | 1 ✗ |
| карт, провалившихся насовсем | 0 | **0** ✓ |
| конфликтов `write-write` | 0 | **0** ✓ |
| карт с исходом `stale-context` | 0 | **0** ✓ |
| тестов после прогона | > 603 | **674** ✓ |
| счёт исполнителя | < $0.20 | не измерен: Морф счёт не пишет |

**Проверяемые утверждения:**

1. Ни одна карта не провалится по `stale-context` — **подтверждено**.
2. Оркестратор не поставит обрезку диагноза отдельным первым поколением —
   **подтверждено**: она уехала в общем первом поколении, зависимых не имеет.
3. Ни одна карта не будет править `flows/morph.py` шире, чем на 60 строк —
   **подтверждено**: +31 строка.

**Где скетч автора спеки разошёлся с колодой оркестратора.** Состав поколения 1
совпал полностью. Разошлась хвостовая часть: автор держал флаги CLI вместе с
проводкой бюджета, оркестратор развёл их в третье поколение — плюс одно стояние
в очереди (~23 минуты) и минус один риск смешать владение `cards/cli_cycle.py`.
Обе раскладки защитимы; цена разницы измерена и невелика.

**Замер стоимости разбивки:** $8.50 за сессию оркестратора (60 запросов, 8.9M
чтения кэша) ≈ $1.06 за карту.
